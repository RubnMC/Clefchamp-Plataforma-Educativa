Every HTTP request flows through Express middleware (logging, body parsing, cookie parsing, session hydration from MySQL), hits a router, passes through an optional isLoggedIn guard, executes a route handler that may call the DAO, and finally renders an EJS template or returns JSON. Two response types coexist: server-rendered HTML pages and JSON API endpoints consumed by client JS.
